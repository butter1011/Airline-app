import 'package:flutter/material.dart';

class ReviewsubmissionScreen extends StatelessWidget {
  const ReviewsubmissionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("ReviewsubmissionScreen"),
    );
  }
}
